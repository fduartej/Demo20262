@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="page-head text-center py-5">
    <span class="eyebrow">Sin acceso</span>
    <h1 class="display-4 mt-2 mb-2">Acceso denegado</h1>
    <p>Lo sentimos, no tienes permisos para ver esta página.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-brand btn-lg px-4 mt-3">Volver al inicio</a>
</div>
